do @print("Prime numbers:\n")
do @print(@format_int(2))

set max, 100
for num, 3, $max + 1:
	set prime_num, true
	set max_factor, @sqrt($num)
	for factor, 2, $max_factor + 1:
		if $num % $factor == 0:
			set prime_num, false
	if $prime_num:
		do @print(", ")
		do @print(@format_int($num))